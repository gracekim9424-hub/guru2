package com.damyeoom.app.ui.theme

import androidx.compose.ui.graphics.Color

// 앱 전체 배경 색상
val BgLight = Color(0xFFF2F3EF)

// 카드와 입력창 배경 색상
val CardGray = Color(0xFFEDEEEA)

// 아이콘 배경 색상
val ChipWhite = Color(0xFFF7F8F6)

// 주요 텍스트 색상
val TextPrimary = Color(0xFF1F2523)

// 보조 텍스트 색상
val TextSecondary = Color(0xFF6B716D)

// 주요 버튼 색상
val ButtonDark = Color(0xFF15181A)

// 비활성 버튼 색상
val ButtonDisabled = Color(0xFFB7BBB4)

// 포인트 색상
val AccentTeal = Color(0xFF0F6B54)

// 구분선 색상
val Divider = Color(0xFFDDDFDA)

// 지도 핀 색상
val PinRed = Color(0xFFE0483E)

// 오류 메시지 색상
val ErrorRed = Color(0xFFD64545)